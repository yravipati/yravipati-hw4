# Python requirements for Part 1 (CSV to SQLite)
# No external dependencies needed - uses only Python standard library
# (sqlite3, csv, os, re, sys are built-in)

# For optional testing with FastAPI (not used in final deployment):
# fastapi>=0.112.0
# uvicorn[standard]>=0.23.0
# pydantic>=2.0.0
